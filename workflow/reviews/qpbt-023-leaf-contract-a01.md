# QPBT-023 A01 immutable-scope supersession handoff

## Terminal disposition

A01 stops without generation or commit because the source-faithful F04 split
changes deterministic chapter outputs outside its immutable ownership. The
same external orchestrator thread may continue only after root archives A01
and canonically issues A04 with the expanded exact path set.

Required continuation: preserve this worktree and diff byte-for-byte, add
`blueprint/src/generated/chapter-03-entries.tex` through
`blueprint/src/generated/chapter-11-entries.tex` to the new session ownership,
inspect the current structured contract, run the full deterministic gates,
commit one candidate, and request two fresh immutable reviews. Do not restart
the source/API investigation.

## Exact identity and timing

- Logical session: `i023-orchestrator-a01-leaf-contract`.
- External path: `/root/i023_orchestrator_a01_leaf_contract`.
- Worktree:
  `/home/drx/MIPStarRE-auto/.workflow-runtime/worktrees/qpbt-023-leaf-contract-a01`.
- Required and observed HEAD:
  `942f9438b991ece8942815db16c019b92d9cdd8e`.
- Required and observed tree:
  `09123f4b25c892a146aabaa77d73cf0c5f35a0c6`.
- UTC start (canonical session issuance):
  `2026-09-01T08:02:30.546949Z`.
- UTC end evidence: `2026-09-01T09:23:46.413233831Z`.
- Measured elapsed: `4875.803` seconds.
- Timing quality: difference of same-host realtime epoch readings at canonical
  issuance and terminal handoff; root lifecycle timing remains authoritative.
- Token usage: `null`.
- Token availability reason: the collaboration backend does not expose
  per-session token usage; no estimate was made.

## Frozen repository diff manifest

Tracked modified paths (9):

```text
blueprint/check.py
blueprint/generated/graph.dot
blueprint/generated/graph.json
blueprint/metadata/gaps.json
blueprint/metadata/nodes.json
blueprint/src/generated/chapter-02-entries.tex
blueprint/src/generated/chapter-12-entries.tex
blueprint/src/generated/gaps.tex
blueprint/tests/test_check.py
```

Untracked owned path (1):

```text
docs/paper-gaps/self-dual-normal-basis.md
```

`git diff --check` exits 0. No Git commit, ref write, merge, rebase, reset,
checkout, stash, or worktree mutation was performed. The currently modified
generated graph/chapter/gap files are the earlier source-only deterministic
generation; after the final 51-node split they are intentionally stale and
must be regenerated by A04.

Current tracked diff stat (the untracked gap note is not included by Git):

```text
blueprint/check.py                             | 211 ++++++++++++++++----
blueprint/generated/graph.dot                  |   2 +-
blueprint/generated/graph.json                 | 104 ++++++++--
blueprint/metadata/gaps.json                   |  10 +
blueprint/metadata/nodes.json                  | 265 +++++++++++++++++++------
blueprint/src/generated/chapter-02-entries.tex |  16 +-
blueprint/src/generated/chapter-12-entries.tex |   6 +-
blueprint/src/generated/gaps.tex               |   1 +
blueprint/tests/test_check.py                  |  98 ++++++++-
9 files changed, 577 insertions(+), 136 deletions(-)
```

## Completed scout evidence

### A02 source integrity

- Stable ID: `i023-scout-a02-source-integrity`.
- Canonical report:
  `workflow/reviews/qpbt-023-source-integrity-a02.md`.
- SHA-256:
  `a52001d4589465b7ffe72e852f1d818d411a8ddbb442b34e5bc0568b7a36d747`.
- Agent monotonic elapsed: `807.24` seconds; root lifecycle elapsed:
  `860.603` seconds.
- Actions: 0 Lean, build, cache, network, endpoint, GitHub, credential,
  repository-write, Git-write, or nested-agent actions.
- Disposition incorporated: separate F01 mathematical existence from K03A's
  uniform algorithm; widen F03 provenance and postprocess/sign equation; split
  F04 finite values from indexed Big-O relations; make PMF marginals and state
  choice explicit; use side-specific conjugation and callable local/tensor
  adapters; record the zero-based `Fin k` Frobenius convention.

### A03 pinned Lean API

- Stable ID: `i023-scout-a03-lean-api`.
- Source report: `/tmp/i023-scout-a03-lean-api.md`.
- SHA-256:
  `6bddc1560c4a2133e8a2ff70aab3e1790c6488469c32212e0bdcafe956cc301c`.
- Monotonic elapsed: `2131.81` seconds.
- Lean probes: 21 total, 12 pass and 9 bounded scouting corrections; every
  requested API group has a clean focused elaboration and the final union
  signature probe passed.
- Actions: 0 full builds, cache commands, network, endpoint, GitHub,
  credential, tracked repository writes, Git writes, placeholders, or nested
  agents. Authorized ignored local materialization commands: 2.
- Disposition incorporated: exact Field and Approximation imports; certified
  `ZMod 2` observable and body equation; field coordinates/multiplication
  matrix; side-specific rectangular conjugation; local lifts; certified tensor
  isometry; state/operator reindexing; finite PMF values/bounds; indexed atTop
  Big-O state/operator/measurement/strategy relations; explicit joint-PMF
  marginals/state choice; finite/indexed consistency; and all four law
  statement contracts.

## Frozen mathematical and workflow decisions

1. F01 uses direct `GaloisField 2 k`. It exposes the exact simultaneous
   self-dual-normal `FieldData` boundary and no caller-supplied basis, witness,
   algorithm, assumptions package, or obligation input.
2. G16 records the missing pinned simultaneous-basis theorem. The minimal
   skeleton permits exactly one G16 hole at
   `MIPStarRE.QPBT.fieldDataOfOddExponent`; the visible nonemptiness theorem
   keeps the source claim callable. K03A separately owns the one uniform
   deterministic algorithm, tables, coherence, and runtime theorem.
3. The minimal skeleton now declares exactly two holes:
   `fieldDataOfOddExponent` (`G16`) and `pauliSoundness` (`main-theorem`). The
   proof-complete count remains zero. No `axiom` or `constant` is permitted.
4. F03 freezes the qualified measurement hierarchy, family postprocess and
   fiber equations, projectivity predicate, certified `BinaryObservable`, and
   exact `effect 0 - effect 1` equation.
5. F04 is now four nodes: finite foundation (`F04-DISTANCE`), indexed relations
   (`F04-ASYMPTOTIC`), consistency (`F04-CONSISTENCY`), and triangle/
   postprocess laws (`F04-DISTANCE-LAWS`). Their dependency closure is valid in
   metadata. Exact structured writer contracts bind Field to
   `MIPStarRE/QPBT/Basic/Field.lean` and every Approximation declaration to
   `MIPStarRE/QPBT/Basic/Approximation.lean`.

## Why ownership expansion is necessary

The old F04 node promised finite values, indexed asymptotics, consistency, and
laws under five names. A02 and A03 independently found that this silently
identified exact `<= delta` with the paper's indexed `O(delta)` and omitted
callable semantics. Splitting the node changes definition-ancestor closures and
direct consumers in chapters 03-11 (external Magic Square/orthonormalization,
appendix prerequisites, LDT adapters, and downstream soundness). The generator
therefore correctly reports chapter 03 through 11 entries stale. Generating
them under A01 would violate immutable ownership, so A01 failed closed.

## Validation and action accounting

- Current JSON parse: pass; 51 unique ordered node records observed.
- Current metadata/dependency/source-root validation: pass. Exact command
  `python3 blueprint/check.py --check --source-root
  /home/drx/MIPStarRE-auto/references/2001.04383v3` reports only 12 expected
  stale outputs: graph JSON/DOT and chapter 02 through chapter 12 entries.
- `git diff --check`: pass.
- Last completed blueprint unit suite before the final node/contract expansion:
  28/28 pass. It must be rerun by A04 against the frozen final diff.
- Earlier workflow validation snapshots: pass; A04 must rerun the canonical
  workflow gate.
- Orchestrator Lean/Lake/build invocations: 0.
- Orchestrator cache warm/seed/status invocations: 0.
- Child sessions: 2, time-sliced one at a time; child-of-child sessions: 0.
- Repository status paths: 10 total (9 tracked modified, 1 untracked owned).
- Git commits/writes: 0.
- Network/endpoint/GitHub/credential operations: 0.
- Generated writes after the expanded-scope discovery: 0.
- Report files written outside the repository: 1 (this handoff).

The handoff SHA-256 is intentionally not embedded. It is computed after the
file is closed and reported out of band.
