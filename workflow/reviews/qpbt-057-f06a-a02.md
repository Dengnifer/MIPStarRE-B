# QPBT-057 executable CL resource repair (A03)

Session: `i057-orchestrator-a03-resource-evidence`
External collaboration identity: `/root/i057_orchestrator_a03_resource_evidence`
Issue: `QPBT-057`
Worktree: `/home/drx/MIPStarRE-auto/.workflow-runtime/worktrees/qpbt-057-resource-repair-a03`
Branch: `issue/qpbt-057-resource-repair-a03`
Starting HEAD: `9982a8574a33967d5de147e2c59a74dc8b4c5d2f`
Starting tree: `196a34c14f3301df9a42047db912e42139bf66d8`
Integration base: `fce3f96becebe568c6487d557aec550499e28840`

## Verdict

The candidate repairs `F057-A07-001` without changing the frozen public API.
The selected private compiler now carries a runtime guarantee for the exact
machine, dependent execution family, and exponent program used by public
`ExecutableCLSampler.downsize`. Current-candidate validation resolves
`F057-A07-002`.

## Source and review authority

- Pinned `conditionally-linear.tex:628-712` defines one downsized machine and
  gives that same machine the `O(TIME_S(n) log q(n))` bound.
- `preliminaries.tex:96-143` fixes the operational Turing-machine and linear
  dual-rail tuple boundary.
- Review A07 SHA-256
  `b20a3f4c9fbe4e0a95a1ce598fa423acd74fd53a746f9043b517bcbdd4526f70`
  identified the correctness-only selection defect and stale validation.
- Paper-integrity review A08 SHA-256
  `d23c525774fa078125479ec473c3aa7bc1e05a6ef094f2d215fbf63ee0213a0c`
  approved the source-facing signatures and exact Stage-4A debt boundary.
- Resource design A05 SHA-256
  `d07b45e9f705cde95f3052930ae05567574dc80df8ab7f59808912db4487de1f`
  supplied the incorporated private certificate shape.

## Repair

`ExecutableCLSampler.compiledDownsize` is a private assembler whose associated
sampler and decomposition are fixed to the mathematical downsize data. Private
`ExecutableCLSampler.DownsizeCompiler S` contains one machine, the dependent
execution family for that machine, one binary-field exponent program, and:

```lean
runtime : forall _hEll : 1 <= ell,
  RuntimeBigO
    (S.compiledDownsize machine execution fieldProgram).time
    (fun n => S.time n * Nat.log 2 (Q.fieldSize n))
```

The private existence theorem now returns `Nonempty (DownsizeCompiler S)` and
retains its authorized Stage-4A `sorry`. Public `downsize` makes one
`Classical.choice` and passes all three projections of that same choice to
`compiledDownsize`. The runtime evidence therefore cannot refer to a different
correct compiler or exponent program.

Both helper declarations are private. The exact 56-name public list, all public
signatures, the four imports, and the public `downsize_time` statement are
unchanged. The only `sorry` tokens remain the authorized theorem bodies
`ExecutableCLSampler.downsizeCompiler_exists` and
`ExecutableCLSampler.downsize_time`; no `admit`, declared `axiom` or `constant`,
generic `Hypotheses`/`Assumptions`, `_ofObligations`, public bridge, residual,
repair, package, producer, witness, or arbitrary implication premise exists.

## Finding dispositions

| Finding | Disposition | Evidence |
| --- | --- | --- |
| `F057-A07-001` | fixed | The resource field is definitionally about the sampler assembled from the selected object's exact machine, executions, and exponent program. |
| `F057-A07-002` | fixed | Scoped Lean, affected targets, full build, blueprint default/pinned synchronization, and workflow checks ran in the assigned current candidate worktree against `Types.lean` SHA-256 `d324bbcb9f54a3de360d18820c4ca0e4258e6a0d94d0cb328b101cd1d3b368bc`. |

## Statement integrity

| Item | Paper assumptions | Lean assumptions | Paper conclusion | Lean conclusion | Verdict |
| --- | --- | --- | --- | --- | --- |
| `ExecutableCLSampler.downsize_time` | Positive indices, admissible field sizes, an executable ell-level sampler, and `ell >= 1` | Unchanged frozen F06A boundary, including intrinsic exponent execution charged in `S.time`; no new public premise | The constructed downsize has runtime `O(TIME_S(n) log q(n))` | Unchanged global-positive `RuntimeBigO S.downsize.time (fun n => S.time n * Nat.log 2 (Q.fieldSize n))` | exact statement; tracked proof debt |
| Private compiler selection | The definition constructs one machine and the lemma bounds that machine | One private selected record bundles machine, executions, exponent program, and the runtime property of their exact assembled sampler | Correct query execution and runtime concern the same construction | The private certificate binds correctness and resources to the same selected data | faithful boundary |

## Validation

| Command | Result |
| --- | --- |
| `python3 scripts/hot_main_cache.py warm` with pinned authenticated archive inputs | pass; current main `c0d5b074380f1c38446023e8aae9eaff1235d69b`, key `f0b82b5dcd154864aed7e77098a203ac19f53e41bda105ac40fb4ad744dfb176`, one elected build, zero lock wait, 8,992 jobs |
| `python3 scripts/hot_main_cache.py --repo-root /home/drx/MIPStarRE-auto prepare --worktree .../qpbt-057-resource-repair-a03` | pass; private seed and authenticated materialization/verification |
| `lake build MIPStarRE.QPBT.Basic.Field` | pass, 2,357 jobs; required because the seed lacked the dependency olean; inherited G16 warning only |
| `lake env lean MIPStarRE/QPBT/Game/Types.lean` | pass; exactly the two authorized `sorry` warnings |
| temporary direct certificate-projection proof of `downsize_time` | pass; confirms definitional identity with the selected assembled sampler; authorized `sorry` restored afterward |
| `lake build MIPStarRE.QPBT.Game.Types` | pass, 3,101 jobs; inherited G16 and flexible-simp diagnostics plus the two authorized warnings |
| `lake build` | pass, 8,992 jobs |
| `python3 blueprint/check.py --check` | pass, 54 nodes and 12 chapters |
| `python3 blueprint/check.py --check --source-root /home/drx/MIPStarRE-auto/references/2001.04383v3` | pass, 54 nodes and 12 chapters |
| `python3 -m unittest discover -s blueprint/tests -p 'test_*.py'` | pass, 36 tests |
| `python3 scripts/workflow.py validate` | pass, 69 issues, 34 PRs, 531 issued sessions, 7 stages |
| `python3 tests/test_check_workflow.py` | pass, 3 tests |
| exact API/import/debt scan and `git diff --check` | pass; 56 names, four imports, exactly two authorized holes |

The first scoped Lean attempt stopped because the seeded main cache did not
contain `MIPStarRE/QPBT/Basic/Field.olean`; the required private Field target
build resolved it. One preliminary `prepare` command omitted the global
`--repo-root` option and failed closed before creating `.lake`; the corrected
command succeeded. Neither event changed source or introduced proof retries.

## Cache and accounting

- Warm: cache miss `1`, cache hit `0`, builds `1`, lock wait `0.0s`, dependency
  cache `40.212703s`, build `614.080216s`, total `731.294630s`.
- Prepare seed: cache hit `1`, cache miss `0`, builds `0`, lock wait `0.0s`,
  copy `117.936150s`, 124,925 private files and 10,097,592,794 bytes;
  materialization `9.004800s`.
- Lean elaboration attempts: `5` total (`1` dependency-missing stop, `4` passes;
  one pass exposed and the next edit removed a private unused-binder lint, one
  pass checked direct runtime-certificate projection, and the final pass
  restored exactly two authorized warnings). Target builds: `2`. Full builds:
  `1`.
- Nested agents spawned by this session: `0`; topology: one orchestrator
  incorporating one independently authenticated A05 design report.
- Reviewer findings addressed: `2`; implementation retries: `0`; incidents:
  `0`; network, endpoint, GitHub, and credential actions: `0`.
- Token usage: `null`; the collaboration backend exposes no per-session token
  count. Session start/total elapsed time is not exposed to this collaboration
  thread; exact cache and command-phase timings are recorded above.

The final commit, tree, report hash, patch hash, and owned-path identities are
returned in `/tmp/i057-orchestrator-a03-resource-evidence.md` after commit to
avoid self-reference.
