# QPBT-057 executable CL minimal skeleton (A02 rebase)

Session: `i057-orchestrator-a02-types-rebase`
Issue: `QPBT-057`
Declared base: `038d878f6e1929d592846edd68a93cc0717d5b02`
Implementation commit (pre-report): `a1333041f9c3264bab619559fa983afd8e7ec803`
Implementation tree: `b5c9dc9021811c524ecf5431a2a1525db6d7a524`
Implementation patch SHA-256 (binary diff, owned Types path): `5d27e5a118e6ef732367340caa3e4bf3abf532778474f4b5f44617f0334135fb`
Types.lean SHA-256: `6fbc0982b64b7b316e7433abb3ef426d95d9da00b0ad53d059b811740f70fb31`

## Scope and reconstruction

The stale QPBT-057 candidate was reconstructed from the reviewed executable
implementation commit chain onto the declared current base. The resulting diff
contains only `MIPStarRE/QPBT/Game/Types.lean` (834 insertions). No workflow
state, research metrics, or unrelated history was replayed.

The file preserves the frozen four-import union:

- `Mathlib.Computability.TuringMachine.Computable`
- `Mathlib.Data.Nat.Log`
- `Mathlib.Probability.Distributions.Uniform`
- `MIPStarRE.QPBT.Basic.Field`

It implements the F06A executable layer and the approved dual-rail
`packSixTapes` body, with the exact 56 public declarations. The only new
tracked proof debt is the two authorized Stage-4A theorem bodies:
`ExecutableCLSampler.downsizeCompiler_exists` and
`ExecutableCLSampler.downsize_time`. No `admit`, declared `axiom`, public
obligation premise, generic `Hypotheses`/`Assumptions`, or
`_ofObligations` helper was introduced.

## Validation

- `lake env lean MIPStarRE/QPBT/Game/Types.lean`: pass; exactly two authorized
  `sorry` warnings (lines 1559 and 1614).
- `lake build MIPStarRE.QPBT.Basic.Field`: pass, 2,357 jobs; inherited G16
  `Field.lean:24` warning only.
- `lake build MIPStarRE.QPBT.Game.Types`: pass, 3,101 jobs; inherited G16
  warning, two authorized F06A warnings, and pre-existing flexible-simp linter
  diagnostics.
- `lake build`: pass, 8,992 jobs.
- `python3 blueprint/check.py --check`: pass (54 nodes, 12 chapters).
- `python3 blueprint/check.py --check --source-root
  /home/drx/MIPStarRE-auto/references/2001.04383v3`: pass.
- `python3 -m unittest discover -s blueprint/tests -p 'test_*.py'`: pass (36/36).
- `python3 scripts/workflow.py validate`: pass (69 issues, 34 PRs, 520
  issued sessions, 7 stages).
- `python3 scripts/check_workflow.py --root . --skip-tests`: pass.
- `git diff --check`: pass.
- Private hot-main cache: key
  `d202af226e2b6804f505deed94453bc1cf2a0a0b6300cb529b73ba89f7976370`, exact
  main `038d878f...`; authenticated foundation verified, zero cache builds,
  zero lock wait, 166.065s seed copy, and 4.657s source materialization.
  The initial scoped check required the one permitted `Field` dependency build.

## Statement integrity

The F06A declarations match the frozen QPBT-059 marker and QPBT-060
Stage-4A boundary: positive-index admissible odd-exponent families, canonical
F01 codec, six-tape dual-rail serialization with blank unused tapes, genuine
execution/decomposition data, exact PMF pushforward and dimension identities,
and global-positive RuntimeBigO. The compiler/execution and runtime arguments
remain exactly the two authorized skeleton holes, linked to QPBT-061 for Stage
4C discharge.

The final report commit will add only this report path; its resulting head/tree
and report SHA-256 are recorded out of band after commit.
