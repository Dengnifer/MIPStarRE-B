# QPBT Lean Blueprint

Stage three will build the dependency-tracked blueprint here after the pinned
paper source has been split and audited. See `protocols/formalization.md` for
the required source anchors, declaration metadata, proof dependencies, and
statement-integrity gates.
